from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='On articles published by Mashable (mashable.com), we determine weather an article is popular or not based on social linking and article features. ',
    author='tingfen yu',
    license='MIT',
)
